---
phase: 10-config-consolidation-and-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fastcode/answer_generator.py
  - .env.example
autonomous: true
requirements:
  - DEBT-04

must_haves:
  truths:
    - "No Python file calls os.getenv(\"MODEL\") or reads the MODEL env var"
    - ".env.example contains exactly one model var (LITELLM_MODEL), no MODEL= line"
    - "answer_generator.py uses llm_client.DEFAULT_MODEL directly for self.model"
    - "A migration note in .env.example explains MODEL is removed"
  artifacts:
    - path: "fastcode/answer_generator.py"
      provides: "Consolidated model assignment"
      contains: "self.model = llm_client.DEFAULT_MODEL"
    - path: ".env.example"
      provides: "Single-variable model configuration"
      contains: "LITELLM_MODEL="
  key_links:
    - from: "fastcode/answer_generator.py"
      to: "fastcode/llm_client.DEFAULT_MODEL"
      via: "direct attribute read at self.model assignment"
      pattern: "self\\.model = llm_client\\.DEFAULT_MODEL"
    - from: ".env.example"
      to: "LITELLM_MODEL env var"
      via: "shell export / dotenv load"
      pattern: "^LITELLM_MODEL="
---

<objective>
Consolidate the MODEL and LITELLM_MODEL dual env var split into a single LITELLM_MODEL var by updating answer_generator.py and .env.example.

Purpose: Eliminates the silent confusion where setting MODEL overrides only the answer generator while all other LLM callers (iterative_agent, query_processor, repo_selector, repo_overview) read LITELLM_MODEL — a user could set MODEL thinking all callers are affected when only one is.

Output: answer_generator.py reads llm_client.DEFAULT_MODEL directly; .env.example has one model var with a migration note; no Python file references os.getenv("MODEL").
</objective>

<execution_context>
@/Users/knakanishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knakanishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-consolidation-and-verification/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove os.getenv("MODEL") from answer_generator.py</name>
  <files>fastcode/answer_generator.py</files>
  <action>
    Edit line 41 of fastcode/answer_generator.py. Change:

      self.model = os.getenv("MODEL") or llm_client.DEFAULT_MODEL

    To:

      self.model = llm_client.DEFAULT_MODEL

    Do NOT remove `import os` — it is still used by other code in the file (os.path calls and commented debug block). Do NOT remove `load_dotenv()` on line 39 — still needed by other code in the file and has no impact on this assignment since llm_client.DEFAULT_MODEL is a module-level constant set at import time (pre-existing behavior, not made worse by this change).

    Verify no other line in fastcode/answer_generator.py calls os.getenv("MODEL") after the edit:
      grep -n 'getenv.*"MODEL"' fastcode/answer_generator.py
    Expected: no output.
  </action>
  <verify>
    <automated>grep -rn 'getenv.*"MODEL"' fastcode/ --include="*.py" ; echo "exit:$?"</automated>
    <manual>Confirm grep returns no matches (only the exit code line). The self.model assignment on line 41 reads llm_client.DEFAULT_MODEL directly.</manual>
  </verify>
  <done>grep -rn 'getenv.*"MODEL"' fastcode/ --include="*.py" returns no output; fastcode/answer_generator.py line 41 reads self.model = llm_client.DEFAULT_MODEL</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example to remove MODEL block and add migration note</name>
  <files>.env.example</files>
  <action>
    Rewrite .env.example to remove the MODEL= block (lines 1-7) and consolidate the LITELLM_MODEL section with a migration note. The final file must look exactly like this:

    ```
    # FastCode Environment Variables

    # === LLM Model ===
    # Model for all LLM callers: answer generator, query processor, repo selector, etc.
    # Must include vertex_ai/ prefix to route through VertexAI with ADC auth.
    # MIGRATION NOTE (v1.2): MODEL env var removed. Set LITELLM_MODEL only.
    LITELLM_MODEL=vertex_ai/gemini-2.0-flash-001

    # === Nanobot Model (Agent Reasoning / Feishu Conversations) ===
    NANOBOT_MODEL=minimax/minimax-m2.1

    # === VertexAI / GCP Configuration ===
    # Required for litellm vertex_ai/ calls via Application Default Credentials
    # Auth: run `gcloud auth application-default login` before use
    VERTEXAI_PROJECT=your-gcp-project-id
    VERTEXAI_LOCATION=us-central1
    # Model format: vertex_ai/<model-name> (always use vertex_ai/ prefix for ADC auth)
    # Example: vertex_ai/gemini-3-flash-preview
    ```

    Key changes from current file:
    - Remove lines 1-14 (the old MODEL= block and the old LITELLM_MODEL section with their verbose dual-var comment)
    - Replace with the consolidated === LLM Model === block shown above
    - Keep NANOBOT_MODEL, VertexAI config, and the trailing example comment unchanged

    Do NOT add MODEL back as a deprecated/commented line — the point is one clean variable.
  </action>
  <verify>
    <automated>grep "^MODEL=" .env.example; echo "MODEL_check:$?"; grep "^LITELLM_MODEL=" .env.example; echo "LITELLM_check:$?"</automated>
    <manual>grep "^MODEL=" returns no output (exit 1). grep "^LITELLM_MODEL=" returns "LITELLM_MODEL=vertex_ai/gemini-2.0-flash-001" (exit 0). MIGRATION NOTE comment is present above LITELLM_MODEL line.</manual>
  </verify>
  <done>grep "^MODEL=" .env.example returns no match; grep "^LITELLM_MODEL=" .env.example returns exactly one line; .env.example contains "MIGRATION NOTE (v1.2)" comment above LITELLM_MODEL</done>
</task>

</tasks>

<verification>
Run both verification commands:

1. grep -rn 'getenv.*"MODEL"' fastcode/ --include="*.py"
   Expected: no output (zero matches)

2. grep "^MODEL=" .env.example
   Expected: no output (exit 1)

3. grep "^LITELLM_MODEL=" .env.example
   Expected: LITELLM_MODEL=vertex_ai/gemini-2.0-flash-001

4. grep "MIGRATION NOTE" .env.example
   Expected: one line containing MIGRATION NOTE (v1.2)

All four checks passing = DEBT-04 complete.
</verification>

<success_criteria>
- No Python file in fastcode/ reads os.getenv("MODEL") — zero grep matches
- .env.example has exactly one model variable: LITELLM_MODEL
- answer_generator.py line 41 reads self.model = llm_client.DEFAULT_MODEL (no os.getenv fallback)
- .env.example contains a migration note explaining MODEL was removed in v1.2
</success_criteria>

<output>
After completion, create .planning/phases/10-config-consolidation-and-verification/10-01-SUMMARY.md
</output>
