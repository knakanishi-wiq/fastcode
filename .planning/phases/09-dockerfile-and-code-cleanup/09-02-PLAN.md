---
phase: 09-dockerfile-and-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fastcode/__init__.py
  - fastcode/retriever.py
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02

must_haves:
  truths:
    - "fastcode/__init__.py contains no OS-detection or TOKENIZERS_PARALLELISM platform import block"
    - "retriever.py line 415 passes task_type='RETRIEVAL_QUERY' as an explicit keyword argument"
    - "No unused import os or import platform remains in __init__.py"
  artifacts:
    - path: "fastcode/__init__.py"
      provides: "Clean module init without dead platform import block"
      min_lines: 25
    - path: "fastcode/retriever.py"
      provides: "Explicit task_type kwarg at embed_text call site"
  key_links:
    - from: "fastcode/retriever.py:415"
      to: "fastcode/embedder.py:embed_text()"
      via: "task_type='RETRIEVAL_QUERY' explicit kwarg"
      pattern: "embed_text.*task_type=\"RETRIEVAL_QUERY\""
---

<objective>
Remove the dead platform import block from fastcode/__init__.py and make the task_type argument explicit at retriever.py line 415.

Purpose: Two items of tech debt from v1.1 — (1) the OS-detection block that set TOKENIZERS_PARALLELISM/OMP_NUM_THREADS/etc. became dead code when sentence-transformers was removed; (2) retriever.py line 415 calls embed_text() without an explicit task_type, hiding the intent. Both are small, targeted edits.

Output: __init__.py with lines 6-13 deleted (no import os, no import platform, no if-block); retriever.py line 415 updated to pass task_type="RETRIEVAL_QUERY" explicitly.
</objective>

<execution_context>
@/Users/knakanishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knakanishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dockerfile-and-code-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead platform import block from fastcode/__init__.py (DEBT-01)</name>
  <files>fastcode/__init__.py</files>
  <action>
    Read /Users/knakanishi/Repositories/FastCode/fastcode/__init__.py. The current file has:

    Lines 1-5: docstring
    Lines 6-13 (DELETE ALL OF THESE):
      import os
      import platform
      (blank line)
      if platform.system() == 'Darwin':
          os.environ['TOKENIZERS_PARALLELISM'] = 'false'
          os.environ['OMP_NUM_THREADS'] = '1'
          os.environ['OPENBLAS_NUM_THREADS'] = '1'
          os.environ['MKL_NUM_THREADS'] = '1'
    Lines 14+: blank line, from .main import FastCode, and the rest of the imports

    Delete lines 6-13 inclusive in a single edit. Do NOT leave any blank lines created by the deletion that would result in more than one consecutive blank line between the docstring and the first `from .main import` line.

    After deletion, the file should begin:
    ```python
    """
    FastCode 2.0 - Repository-Level Code Understanding System
    With Multi-Repository Support
    """

    from .main import FastCode
    from .loader import RepositoryLoader
    ...
    ```

    The `import os` and `import platform` lines must both be removed — they are only used by the dead block. Leaving either as an unused import would fail linting (F401).

    Note from research (Pitfall 5): Delete all six lines (6 through 13 inclusive), not just the if-block.
  </action>
  <verify>
    <automated>cd /Users/knakanishi/Repositories/FastCode && python -c "import fastcode; print('import OK')" && grep -c "import os" fastcode/__init__.py; grep -c "import platform" fastcode/__init__.py; grep -c "TOKENIZERS_PARALLELISM" fastcode/__init__.py</automated>
    <manual>Expected: "import OK", then 0, 0, 0 for the grep counts. The module must still import cleanly after the deletion.</manual>
  </verify>
  <done>
    - fastcode/__init__.py imports successfully (python -c "import fastcode" exits 0)
    - No `import os` in __init__.py
    - No `import platform` in __init__.py
    - No `TOKENIZERS_PARALLELISM` string in __init__.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Make task_type explicit at retriever.py line 415 (DEBT-02)</name>
  <files>fastcode/retriever.py</files>
  <action>
    Read /Users/knakanishi/Repositories/FastCode/fastcode/retriever.py around line 415. The current line reads:

    ```python
    query_embedding = self.embedder.embed_text(semantic_query_text)
    ```

    Change it to:

    ```python
    query_embedding = self.embedder.embed_text(semantic_query_text, task_type="RETRIEVAL_QUERY")
    ```

    This is a one-line change. The `embed_text()` signature in fastcode/embedder.py line 28 is:
    ```python
    def embed_text(self, text: str, task_type: str = "RETRIEVAL_QUERY") -> np.ndarray:
    ```

    The explicit kwarg makes the intent visible at the call site and matches the existing default exactly — no behavior change.

    CRITICAL: Use `task_type="RETRIEVAL_QUERY"` (uppercase) — NOT `task_type="retrieval_query"`. The existing default in embedder.py uses uppercase; passing lowercase would cause a task_type validation error at runtime.

    This is a surgical single-line edit. Do not change anything else in retriever.py.
  </action>
  <verify>
    <automated>cd /Users/knakanishi/Repositories/FastCode && grep -n "embed_text" fastcode/retriever.py | head -5</automated>
    <manual>The line at ~415 must show: self.embedder.embed_text(semantic_query_text, task_type="RETRIEVAL_QUERY") — with uppercase RETRIEVAL_QUERY and the exact kwarg name task_type.</manual>
  </verify>
  <done>
    - retriever.py at the semantic search line contains `task_type="RETRIEVAL_QUERY"` as an explicit keyword argument
    - The string is uppercase (RETRIEVAL_QUERY not retrieval_query)
    - No other lines in retriever.py were modified
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify Phase 9 DEBT requirements:

1. DEBT-01: `python -c "import fastcode; print('OK')"` returns OK; `grep "import platform" fastcode/__init__.py` returns no match
2. DEBT-02: `grep "task_type=\"RETRIEVAL_QUERY\"" fastcode/retriever.py` returns a match at the embed_text call site near line 415
</verification>

<success_criteria>
- fastcode/__init__.py has no import os, import platform, or platform.system() block
- fastcode module still imports cleanly after deletion
- retriever.py line ~415 has task_type="RETRIEVAL_QUERY" as explicit kwarg (uppercase)
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-dockerfile-and-code-cleanup/09-02-SUMMARY.md`
</output>
