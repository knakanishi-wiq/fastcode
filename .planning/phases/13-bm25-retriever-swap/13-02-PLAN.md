---
phase: 13-bm25-retriever-swap
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - fastcode/retriever.py
  - fastcode/main.py
  - pyproject.toml
autonomous: true
requirements:
  - BM25-03

must_haves:
  truths:
    - "No `{repo_name}_bm25.pkl` file is written or read anywhere in the codebase"
    - "`rank-bm25` is removed from `pyproject.toml` dependencies"
    - "`import pickle` and `from rank_bm25 import BM25Okapi` are deleted from retriever.py and main.py"
    - "`save_bm25()`, `load_bm25()`, and `index_for_bm25()` methods are deleted from HybridRetriever"
    - "`build_repo_overview_bm25()` and `_select_relevant_repositories()` work without BM25Okapi (simple token-frequency scorer)"
    - "All tests pass after removal; uv sync completes without rank-bm25 in lockfile"
  artifacts:
    - path: "fastcode/retriever.py"
      provides: "Cleaned retriever with no pkl or rank_bm25 references"
    - path: "fastcode/main.py"
      provides: "Cleaned main.py with no pkl BM25 calls"
    - path: "pyproject.toml"
      provides: "rank-bm25 removed from dependencies"
  key_links:
    - from: "fastcode/retriever.py:build_repo_overview_bm25"
      to: "fastcode/retriever.py:_simple_bm25_scores"
      via: "internal scorer call"
      pattern: "_simple_bm25_scores"
    - from: "fastcode/main.py"
      to: "fastcode/retriever.py"
      via: "no pkl method calls"
      pattern: "save_bm25|load_bm25|index_for_bm25"
---

<objective>
Remove all pkl BM25 infrastructure: delete `save_bm25()`, `load_bm25()`, `index_for_bm25()`,
all pkl call sites in `main.py`, the `rank-bm25` dependency, and replace `repo_overview_bm25`
(the only remaining `BM25Okapi` usage) with a simple Python token-frequency scorer.

Purpose: Eliminate the `{repo_name}_bm25.pkl` file ecosystem entirely. After this plan, BM25
retrieval is 100% SQLite FTS5 (from Plan 01) and no external BM25 library is needed.

Output: Clean `retriever.py`, `main.py`, `pyproject.toml` with no pkl or rank_bm25 references.
</objective>

<execution_context>
@/Users/knakanishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knakanishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bm25-retriever-swap/13-CONTEXT.md
@.planning/phases/13-bm25-retriever-swap/13-RESEARCH.md
@.planning/phases/13-bm25-retriever-swap/13-01-SUMMARY.md
@fastcode/retriever.py
@fastcode/main.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pkl methods and pkl imports from retriever.py; replace repo_overview_bm25</name>
  <files>fastcode/retriever.py</files>
  <action>
    Make the following changes to `fastcode/retriever.py`. Read the file first; apply changes
    precisely without modifying unrelated code.

    **A. Remove imports (top of file):**
    - Delete `import pickle` (line ~7)
    - Delete `from rank_bm25 import BM25Okapi` (line ~11)

    **B. Delete `index_for_bm25()` method entirely** (~lines 97-141):
    The method builds `self.full_bm25_corpus` and `self.full_bm25` (BM25Okapi). Delete the whole
    method definition. `self.full_bm25_elements` is still populated by callers — check main.py
    and keep any code that sets `self.retriever.full_bm25_elements = ...` in place.
    NOTE: After Plan 01 removed `self.full_bm25_corpus` and `self.full_bm25` attributes from
    `__init__`, the method body will still reference them — deleting the whole method is correct.

    **C. Delete `save_bm25()` method entirely** (~lines 1229-1250).

    **D. Delete `load_bm25()` method entirely** (~lines 1252-1282).

    **E. Replace `build_repo_overview_bm25()` with a version using a simple token-frequency scorer:**

    The current method uses `self.repo_overview_bm25 = BM25Okapi(...)`. Replace the entire method
    with one that stores tokenized corpus and a simple term-frequency scorer. The corpus is tiny
    (one text per repo, typically <10 repos) so a simple approach is correct.

    Add a private helper method `_simple_bm25_scores` before `build_repo_overview_bm25`:
    ```python
    def _simple_bm25_scores(self, corpus: List[List[str]], query_tokens: List[str]) -> List[float]:
        """Compute term-frequency scores for a small corpus.

        Uses simple TF sum (count of query tokens present in each document).
        Adequate for tiny repo-overview corpora (<20 repos).

        Args:
            corpus: List of tokenized documents.
            query_tokens: Tokenized query.

        Returns:
            List of float scores, one per document (higher = more relevant).
        """
        query_set = set(query_tokens)
        return [sum(1 for t in doc if t in query_set) for doc in corpus]
    ```

    Replace `build_repo_overview_bm25()` body: keep everything except the last three lines that
    set `self.repo_overview_bm25_corpus`, `self.repo_overview_names`, and
    `self.repo_overview_bm25 = BM25Okapi(...)`. New last line:
    ```python
    self.repo_overview_bm25 = True  # Sentinel: corpus is ready; scoring via _simple_bm25_scores
    ```
    Keep `self.repo_overview_bm25_corpus` and `self.repo_overview_names` assignments unchanged.

    **F. Update `_select_relevant_repositories()` BM25 scoring block** (~lines 424-444):
    Replace:
    ```python
    scores = self.repo_overview_bm25.get_scores(query_tokens)
    ```
    With:
    ```python
    scores = self._simple_bm25_scores(self.repo_overview_bm25_corpus, query_tokens)
    ```
    The `if self.repo_overview_bm25 is not None` guard remains (now checks the `True` sentinel).

    **G. Update `reload_for_repos()` pkl-loading block** (~lines 1164-1193):
    The block loads pkl files into `filtered_bm25_elements` and `filtered_bm25_corpus`, then
    does `self.filtered_bm25 = BM25Okapi(all_bm25_corpus)`. After Plan 01 removed `filtered_bm25`
    and `filtered_bm25_corpus` attributes, this block will reference dead attributes.
    Replace the entire pkl-loading block (from `# Reload BM25 indexes...` to
    `self.filtered_bm25 = BM25Okapi(all_bm25_corpus)`) with a single log line:
    ```python
    self.logger.info("BM25 scoping is now handled by FTS5 source_path filtering in full_bm25()")
    ```
    Keep the `self.iterative_agent.bm25_elements = self.filtered_bm25_elements` line and
    the repo-stats update block that follows — those still use `filtered_bm25_elements` for
    element-list iteration (not for BM25 scoring).

    **H. In `__init__`, remove remaining BM25Okapi attribute sentinel:**
    - Remove `self.repo_overview_bm25 = None` (replace with nothing; `build_repo_overview_bm25()`
      sets it to `True` when called)
    - Remove `self.repo_overview_bm25_corpus = []` — KEEP this as `[]` init (still used for scorer)
    - Keep `self.repo_overview_names = []` (still used)

    After all changes run: `python -m pytest tests/ -x -q`
    Must pass. Then verify no pkl references remain:
    `grep -n "pickle\|BM25Okapi\|rank_bm25\|\.pkl" fastcode/retriever.py`
    Expected: zero matches.

    Commit: `feat(13-02): remove pkl BM25 methods and rank_bm25 from retriever.py`
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q && grep -n "pickle\|BM25Okapi\|rank_bm25\|\.pkl" fastcode/retriever.py | wc -l</automated>
    <manual>grep count must be 0; all tests pass.</manual>
    <sampling_rate>run after each sub-step (import removal, method deletion, etc.) to catch errors early</sampling_rate>
  </verify>
  <done>
    retriever.py has no pickle, BM25Okapi, rank_bm25, or .pkl references;
    all tests pass; commit exists with feat(13-02) prefix
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove pkl call sites from main.py and rank-bm25 from pyproject.toml</name>
  <files>fastcode/main.py, pyproject.toml</files>
  <action>
    **A. main.py changes:**

    Read `fastcode/main.py`. Remove all pkl-related BM25 code.

    Deletions (use grep to confirm line numbers before editing):
    - `from rank_bm25 import BM25Okapi` — delete import
    - `import pickle` if present — delete (verify with grep first)
    - All calls to `self.retriever.index_for_bm25(elements)` — delete each call AND the comment
      above it if the comment only describes that call (e.g., "# Index for BM25")
    - All calls to `self.retriever.save_bm25(repo_name)` — delete each call AND surrounding comment
    - All calls to `self.retriever.load_bm25(cache_name)` — delete the call; also delete any
      `bm25_loaded = ...` assignment and `if not bm25_loaded:` block that depends on it
    - `self.retriever.full_bm25_elements = all_bm25_elements` (line ~1197) — DELETE (pkl-derived)
    - `self.retriever.full_bm25_corpus = all_bm25_corpus` (line ~1198) — DELETE (pkl-derived)
    - `self.retriever.full_bm25 = BM25Okapi(all_bm25_corpus)` (line ~1199) — DELETE
    - All pkl-reading blocks in `_try_load_multi_repo_cache()` (~lines 1165-1193) that read
      `{repo_name}_bm25.pkl` — delete the entire per-repo pkl-loading loop body
      (keep the loop structure if it also loads graphs; delete only the bm25 sub-block)
    - Keep `self.retriever.build_repo_overview_bm25()` calls — these remain valid
    - Keep any `self.retriever.full_bm25_elements = elements` assignments that come from
      non-pkl sources (e.g., reconstructed from metadata) — check if they still make sense
      given that `full_bm25_elements` is now only used for element iteration

    IMPORTANT: Grep all pkl BM25 sites before editing to avoid missing any:
    ```
    grep -n "index_for_bm25\|save_bm25\|load_bm25\|BM25Okapi\|rank_bm25\|bm25\.pkl\|_bm25\.pkl" fastcode/main.py
    ```
    Remove every hit. After removal, verify zero hits remain.

    Also check for `all_bm25_elements` / `all_bm25_corpus` local variables — delete them if
    they exist only to feed deleted BM25Okapi constructions.

    Cleanup: if `bm25_loaded` local variable is now unused, delete it and any dead `if not bm25_loaded:` block.

    **B. pyproject.toml changes:**

    Remove `"rank-bm25"` from the `[project] dependencies` list.
    Verify the surrounding entries still have correct comma separation.

    **C. Regenerate lockfile:**
    Run `uv lock` to update `uv.lock` with rank-bm25 removed.
    Then run `uv sync` to verify the environment installs cleanly.

    **D. Final verification:**
    ```
    grep -rn "pickle\|BM25Okapi\|rank_bm25\|_bm25\.pkl\|index_for_bm25\|save_bm25\|load_bm25" \
      fastcode/retriever.py fastcode/main.py pyproject.toml
    ```
    Expected: zero lines.

    Run full test suite: `python -m pytest tests/ -x -q`

    Commit: `feat(13-02): remove rank-bm25 dep and all pkl BM25 call sites from main.py`
  </action>
  <verify>
    <automated>grep -rn "pickle\|BM25Okapi\|rank_bm25\|_bm25\.pkl\|index_for_bm25\|save_bm25\|load_bm25" fastcode/retriever.py fastcode/main.py pyproject.toml | wc -l && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
    <manual>grep count must be 0; all tests pass; uv sync succeeds.</manual>
    <sampling_rate>run after main.py edits, then again after pyproject.toml + uv lock</sampling_rate>
  </verify>
  <done>
    Zero pkl/BM25Okapi/rank_bm25 references in retriever.py, main.py, pyproject.toml;
    uv.lock updated; full test suite green;
    commit exists with feat(13-02) prefix
  </done>
</task>

</tasks>

<verification>
Phase 13 completion check:
1. `python -m pytest tests/ -x -q` → all pass
2. `python -m pytest tests/test_retriever_bm25.py -v` → 5 tests for BM25-01/BM25-02 pass
3. `grep -rn "rank_bm25\|BM25Okapi\|_bm25\.pkl\|save_bm25\|load_bm25\|index_for_bm25" fastcode/` → zero hits
4. `grep "rank-bm25" pyproject.toml` → zero hits
5. `uv sync` → exits 0
6. `grep "def full_bm25" fastcode/retriever.py` → method found
7. `grep "chunks_fts MATCH" fastcode/retriever.py` → FTS5 query found
8. `ls ./data/*_bm25.pkl 2>/dev/null` → no pkl files written during any test
</verification>

<success_criteria>
- No `{repo_name}_bm25.pkl` file is written during indexing or read during retrieval
- `rank-bm25` is removed from pyproject.toml and uv.lock
- `import pickle` removed from retriever.py (rank_bm25 was its only use in that file)
- `save_bm25()`, `load_bm25()`, `index_for_bm25()` methods no longer exist
- `repo_overview_bm25` uses simple Python scorer (no external library)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-bm25-retriever-swap/13-02-SUMMARY.md`
</output>
