---
phase: 13-bm25-retriever-swap
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - fastcode/retriever.py
  - fastcode/main.py
  - mcp_server.py
  - pyproject.toml
autonomous: true
requirements: [BM25-03]

must_haves:
  truths:
    - "No pkl file is written or read for BM25 during indexing or retrieval"
    - "rank-bm25 is removed from pyproject.toml dependencies"
    - "remove_repository() in main.py no longer includes {repo_name}_bm25.pkl in file_patterns"
    - "All pkl-related BM25 imports (pickle, rank_bm25) are removed from retriever.py"
  artifacts:
    - path: "fastcode/retriever.py"
      provides: "No save_bm25/load_bm25/index_for_bm25 methods, no pickle/BM25Okapi imports"
    - path: "fastcode/main.py"
      provides: "No calls to save_bm25/load_bm25/index_for_bm25; pkl file not listed in remove_repository"
    - path: "pyproject.toml"
      provides: "rank-bm25 removed from dependencies list"
  key_links:
    - from: "fastcode/main.py"
      to: "fastcode/retriever.py"
      via: "retriever.save_bm25 / load_bm25 / index_for_bm25 calls removed"
      pattern: "save_bm25|load_bm25|index_for_bm25"
---

<objective>
Remove all pkl-based BM25 infrastructure: dead methods in retriever.py, call sites in main.py,
artifact references in mcp_server.py and remove_repository(), and the rank-bm25 dependency.
BM25 corpus is now entirely in SQLite — no pkl files are ever written or read.

Purpose: BM25-03 requires zero pkl files for BM25 after this phase.
Output: Clean codebase with no rank_bm25/pickle BM25 code and rank-bm25 removed from pyproject.toml.
</objective>

<execution_context>
@/Users/knakanishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knakanishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bm25-retriever-swap/13-CONTEXT.md
@.planning/phases/13-bm25-retriever-swap/13-01-SUMMARY.md
@fastcode/retriever.py
@fastcode/main.py
@mcp_server.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead BM25 pkl methods and imports from retriever.py</name>
  <files>fastcode/retriever.py</files>
  <action>
    After Plan 01, _keyword_search no longer uses BM25Okapi or pickle. Now remove the dead code:

    1. Remove `import pickle` (top of file, line 7).
    2. Remove `from rank_bm25 import BM25Okapi` (line 11).
    3. Remove method `index_for_bm25(self, elements)` entirely (lines ~97–141).
    4. Remove method `build_repo_overview_bm25(self)` entirely (lines ~143–182) — this method
       builds the in-memory repo_overview_bm25 using BM25Okapi; it is not needed after FTS5 swap.
       Verify: grep for build_repo_overview_bm25 call sites in main.py — if callers exist, remove
       those call sites in Task 2. The repo_overview BM25 (used for repo selection) is separate
       from the full_bm25 path targeted by BM25-01/02/03. If build_repo_overview_bm25 is called
       in main.py, remove those calls and the repo_overview_bm25 attribute usage (repo selection
       falls back to embedding-based selection which already exists).
    5. Remove method `save_bm25(self, name)` entirely (~lines 1229–1250).
    6. Remove method `load_bm25(self, name)` entirely (~lines 1252–1289).
    7. In `__init__`, remove instance attributes:
       - self.full_bm25 = None
       - self.full_bm25_corpus = []
       - self.full_bm25_elements = []
       - self.filtered_bm25 = None
       - self.filtered_bm25_corpus = []
       - self.filtered_bm25_elements = []
       - self.repo_overview_bm25 = None
       - self.repo_overview_bm25_corpus = []
       - self.repo_overview_names = []
       Keep self._db_path and self._db_conn (added in Plan 01).
    8. In `reload_specific_repositories()` method (~line 1164–1227), remove the pkl loading block:
       "# Reload BM25 indexes for specific repositories into FILTERED indexes" through the
       "# Rebuild FILTERED BM25 with selected repositories" block (the BM25Okapi rebuild). Also
       remove the "self.iterative_agent.bm25_elements = self.filtered_bm25_elements" line and
       related self.filtered_bm25_elements references that now no longer exist.
    9. In `_keyword_search`, the method body was already replaced in Plan 01 — verify it has no
       remaining references to full_bm25 or filtered_bm25 attributes.
    10. Scan all remaining method bodies for any remaining reference to full_bm25_elements,
        filtered_bm25_elements, or bm25_elements. For each:
        - If used to pass to IterativeAgent: replace with [] (empty list — agent degrades gracefully)
        - If used in repo_stat calculation: replace with empty list or remove the branch
    11. After edits: uv run python -c "from fastcode.retriever import HybridRetriever" must
        import cleanly (no ImportError). Then run full test suite.

    Commit: feat(13-02): remove pkl BM25 methods and imports from retriever.py — BM25-03
  </action>
  <verify>
    <automated>cd /Users/knakanishi/Repositories/FastCode && uv run python -c "from fastcode.retriever import HybridRetriever; print('OK')" && uv run pytest tests/test_retriever_fts5.py tests/test_indexer_sqlite.py -v 2>&1 | tail -20</automated>
    <manual>No import errors; all 15 tests pass; grep for "BM25Okapi\|rank_bm25\|import pickle" in retriever.py returns nothing</manual>
  </verify>
  <done>
    retriever.py imports cleanly. grep "BM25Okapi\|rank_bm25\|import pickle" fastcode/retriever.py
    returns no results. All tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove pkl BM25 call sites from main.py, mcp_server.py, and pyproject.toml</name>
  <files>fastcode/main.py, mcp_server.py, pyproject.toml</files>
  <action>
    Remove all remaining pkl BM25 infrastructure from callers and the dependency declaration.

    fastcode/main.py:
    1. Remove `import pickle` if present (search top of file).
    2. Remove all calls to `self.retriever.save_bm25(...)` — grep for save_bm25.
       Found at: ~line 250 (after indexing loop), ~line 966 (temp_retriever.save_bm25).
       Delete those lines and any surrounding "if persist:" guards that become empty.
    3. Remove all calls to `self.retriever.load_bm25(...)` — grep for load_bm25.
       Found at: ~line 642. Delete the load attempt and the fallback `index_for_bm25` rebuild.
       The BM25 index is now always available from SQLite — no load step needed at startup.
    4. Remove all calls to `self.retriever.index_for_bm25(...)` — grep for index_for_bm25.
       Found at: ~lines 239, 661, 965, 1207. Delete each call and any immediately surrounding
       comment "# Index for BM25" or "# Rebuild BM25 index" that become orphaned.
    5. Remove calls to `self.retriever.build_repo_overview_bm25()` if present.
    6. In the pkl BM25 loading block within `_load_repos()` or similar (~lines 1164–1173):
       Remove the bm25_path / pickle.load block for per-repo BM25 loading.
       Remove any BM25Okapi rebuild (all_bm25_corpus / all_bm25_elements aggregation).
    7. Remove `f"{repo_name}_bm25.pkl"` from the `file_patterns` list in `remove_repository()`
       (~line 1319). The pkl file will no longer exist, so listing it is dead code.
    8. After each deletion, verify the surrounding code still makes syntactic sense
       (no dangling if-blocks or unreferenced variables).

    mcp_server.py:
    9. The `_is_repo_indexed()` helper checks for `.faiss` + `_metadata.pkl` — that logic is
       about FAISS metadata, not BM25 pkl, so leave it alone.
    10. The `delete_repo_metadata` docstring mentions `_bm25.pkl` — update the docstring to
        remove that reference since bm25.pkl no longer exists. Change:
        "removes vector/BM25/graph index artifacts and the repository's overview entry from
        repo_overviews.pkl" → "removes vector/graph index artifacts and the repository's
        overview entry from repo_overviews.pkl"

    pyproject.toml:
    11. Remove `"rank-bm25",` from the `dependencies` list. Run `uv sync` after removal to
        update uv.lock and confirm the package is no longer resolved.

    Final validation:
    - uv run python -c "from fastcode.main import FastCodeSystem; print('OK')" — no ImportError
    - grep -r "rank_bm25\|BM25Okapi\|_bm25\.pkl\|save_bm25\|load_bm25\|index_for_bm25" \
        fastcode/ mcp_server.py pyproject.toml — zero results
    - uv run pytest tests/ -v — all tests pass

    Commit: feat(13-02): remove rank-bm25 dependency and all pkl BM25 call sites — BM25-03
  </action>
  <verify>
    <automated>cd /Users/knakanishi/Repositories/FastCode && uv run python -c "from fastcode.main import FastCodeSystem; print('OK')" && uv run pytest tests/ -v 2>&1 | tail -30</automated>
    <manual>
      Run: grep -r "rank_bm25\|BM25Okapi\|_bm25\.pkl\|save_bm25\|load_bm25\|index_for_bm25" fastcode/ mcp_server.py pyproject.toml
      Expected: zero output (no matches).
      Confirm rank-bm25 is absent from pyproject.toml dependencies list.
    </manual>
  </verify>
  <done>
    Zero grep matches for BM25 pkl code across fastcode/, mcp_server.py, pyproject.toml.
    All tests pass. rank-bm25 absent from pyproject.toml. uv.lock updated.
  </done>
</task>

</tasks>

<verification>
1. uv run pytest tests/ -v → all tests pass
2. grep -r "rank_bm25\|BM25Okapi\|import pickle" fastcode/ → zero results
3. grep "rank-bm25" pyproject.toml → zero results
4. grep "_bm25\.pkl" fastcode/main.py mcp_server.py → zero results
5. uv run python -c "from fastcode.retriever import HybridRetriever; from fastcode.main import FastCodeSystem; print('OK')"
</verification>

<success_criteria>
1. No BM25Okapi, rank_bm25, or pickle BM25 code anywhere in fastcode/ or mcp_server.py
2. rank-bm25 removed from pyproject.toml and uv.lock reflects removal
3. remove_repository() no longer lists {repo_name}_bm25.pkl
4. All existing tests pass (BM25-03 satisfied)
</success_criteria>

<output>
After completion, create .planning/phases/13-bm25-retriever-swap/13-02-SUMMARY.md
</output>
